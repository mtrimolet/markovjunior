export module engine.model;

import std;

import engine.rewriterule;
export import engine.runner;

using Unions  = RewriteRule::Unions;

export
struct Model {
  // std::string title;
  std::string symbols;
  Unions unions;
  bool origin;
  NodeRunner program;
  bool halted = false;
};
